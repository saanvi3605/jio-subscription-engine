server.port=8087

spring.application.name=payment-method-service

# MariaDB — own DB, separate from jio_payment
spring.datasource.url=jdbc:mariadb://localhost:3306/jio_payment_method?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=Saanvi@999
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MariaDBDialect

# Swagger UI
springdoc.swagger-ui.path=/swagger-ui/index.html
springdoc.api-docs.path=/v3/api-docs

# Downstream service URLs
services.tmf629.url=http://localhost:8083
